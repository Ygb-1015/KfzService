server:
  port: 8095

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pdd
    username: roo
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: 111.229.25.150  # Redis服务器地址
    port: 6379  # Redis服务器端口
    database: 1 # 默认数据库索引，可根据需要指定不同的数据库
    password: Qq123123  # 如果设置了密码，请在此处填写
    timeout: 10s  # 连接超时时间
    lettuce:
      pool:
        max-active: 8 # 最大连接数
        max-idle: 8   # 最大空闲连接数
        min-idle: 0   # 最小空闲连接数

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.pdd.ordersync.entity
  configuration:
    map-underscore-to-camel-case: true

forest:
  max-connections: 1000        # 连接池最大连接数
  connect-timeout: 3000        # 连接超时时间，单位为毫秒
  read-timeout: 3000           # 数据读取超时时间，单位为毫秒





